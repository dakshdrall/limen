/**
 * X uses the same card as everywhere else — one image to keep correct rather
 * than two that drift. Re-exported rather than duplicated.
 */
export { default, alt, size, contentType } from './opengraph-image';
